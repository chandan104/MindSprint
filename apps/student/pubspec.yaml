name: mindsprint_student
description: "MindSprint student app — teacher-supervised cognitive assessments."
publish_to: 'none'

version: 0.3.0+3

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.0
  go_router: ^16.0.0
  supabase_flutter: ^2.8.0
  drift: ^2.20.0
  drift_flutter: ^0.2.0
  uuid: ^4.4.0
  connectivity_plus: ^7.0.0
  flutter_secure_storage: ^9.2.0
  crypto: ^3.0.3
  package_info_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.0
  mocktail: ^1.0.0
  fake_async: ^1.3.0
  sqlite3: ^2.4.0

flutter:
  uses-material-design: true
